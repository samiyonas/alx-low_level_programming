#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 * Return: Always 0 (success)
 */
int main(void)
{
	int i;

	i = 0;
	while (i < 10)
	{
		printf("%d\n", i);
		i++;
	}
	return (0);
}
