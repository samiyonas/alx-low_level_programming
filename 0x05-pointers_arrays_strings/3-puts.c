#include "main.h"

/**
 * _puts - prints a string.
 * @str: pointer variable.
 * Return: nothing.
 */
void _puts(char *str)
{
	int i;

	for (i = 0; *(s + i) != '\0'; i++)
	{
		_putchar(*(s + i));
	}
}
